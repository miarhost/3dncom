<%= form_with(model: [ @topicthread, @branch ], local: true) do |form| %>
  <% if branch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(branch.errors.count, "error") %> prohibited this thread from being saved:</h2>

      <ul>
      <% branch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :branch_title %>
    <%= form.text_field :title, id: :branch_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>